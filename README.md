Run this project in Docker.  <BR>My Dockerfile/compose is included.<BR>
### Building and running the application<BR>
Start the application by running:<BR>
`docker compose up --build`<BR>
Your application will be available at http://localhost.<BR>
